global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # TODO: backend 接入 /metrics 端点后取消注释
  # - job_name: 'brandpulse-backend'
  #   static_configs:
  #     - targets: ['host.docker.internal:8000']
